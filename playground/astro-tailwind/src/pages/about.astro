---
import BaseLayout from "../layouts/BaseLayout.astro";

const name = "TechAkayy";
const roles = [
  "Co-Founder of Vue Designer",
  "Core contributor at Pinegrow",
  "Creator of VueCast for Astro",
];
---

<BaseLayout>
  <section class="bg-white dark:bg-gray-900">
    <div class="max-w-4xl mx-auto px-6 py-16 text-center">

      <!-- Back button -->
      <div class="mb-10 text-left">
        <a
          href="/"
          class="inline-flex items-center px-5 py-2.5 text-sm font-medium text-white rounded-lg bg-primary-700 hover:bg-primary-800 focus:ring-4 focus:ring-primary-300 dark:focus:ring-primary-900"
        >
          ← Back to Home
        </a>
      </div>

      <h1 class="text-4xl font-extrabold text-gray-900 dark:text-white mb-4">
        About VueCast
      </h1>

      <p class="text-lg text-gray-600 dark:text-gray-300 mb-12">
        VueCast brings Vue-style template syntax to Astro — letting you write
        expressive templates without JSX, hydration overhead, or framework lock-in.
      </p>

      <!-- Author Card -->
      <div class="mx-auto max-w-2xl bg-gray-50 dark:bg-gray-800 rounded-xl p-8 mb-12">
        <h2 class="text-2xl font-semibold text-gray-900 dark:text-white mb-2">
          Built by {{ name }}
        </h2>

        <p class="text-gray-600 dark:text-gray-300 mb-6">
          Co-Founder of
          <a
            href="https://vuedesigner.com"
            class="text-primary-600 hover:underline"
            target="_blank"
          >
            Vue Designer
          </a>
          and part of the
          <span class="font-medium">Pinegrow</span> team.
          Passionate about building tools that make UI development simpler
          and more expressive.
        </p>

        <ul class="text-left inline-block">
          <li
            v-for="(role, i) in roles"
            :key="i"
            class="flex items-center gap-2 text-gray-700 dark:text-gray-300 mb-2"
          >
            <span class="w-2 h-2 bg-primary-600 rounded-full"></span>
            {{ role }}
          </li>
        </ul>
      </div>

      <!-- Philosophy -->
      <div class="max-w-3xl mx-auto text-left">
        <h3 class="text-xl font-semibold text-gray-900 dark:text-white mb-3">
          Why VueCast?
        </h3>

        <p class="text-gray-600 dark:text-gray-400 mb-4">
          Astro is great for performance, but JSX isn’t always the most
          ergonomic authoring format. VueCast allows Vue-style templates
          to work natively inside Astro.
        </p>

        <p class="text-gray-600 dark:text-gray-400">
          No runtime. No hydration. Just clean, compile-time transformations.
        </p>
      </div>

    </div>
  </section>
</BaseLayout>

<style is:global>
  @import "../assets/css/tailwind.css";
</style>
