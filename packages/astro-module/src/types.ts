import type { AstroIntegration, HookParameters } from 'astro'
import type { Plugin } from 'vite'

export interface VuecastAstroPluginOptions {
  // Add any plugin options here in the future
}

export type { AstroIntegration, HookParameters, Plugin }
